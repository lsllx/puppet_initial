node '<%=@id%>' {
  include 'myfw'
  <% @settings.each { |key,values|%>
  import '<%=values%>'<% "\n"%>
  <%}%>
  class{ 'docker':
    socket_bind => 'unix://var/run/docker.sock --insecure-registry 10.1.11.194:5000'
  }
  #firewall_settings 
  #all these settings between start and end were created by auto system
  #start
  #end
}
